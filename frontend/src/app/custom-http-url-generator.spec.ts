import { CustomHttpUrlGenerator } from './custom-http-url-generator';

describe('CustomHttpUrlGenerator', () => {
  it('should create an instance', () => {
    expect(new CustomHttpUrlGenerator()).toBeTruthy();
  });
});
